#!/bin/bash
set -euo pipefail
cd /workspace
bundle check
bin/rails db:prepare
bin/rails db:seed
RAILS_ENV=test bin/rails db:prepare
git config user.name 'Benchmark Trial'
git config user.email 'trial@benchmark.invalid'
# Fizzy's setup installs host package managers and hooks. This image owns dependencies.
